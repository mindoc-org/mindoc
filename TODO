1、把 log 提取出 dbgout 之类的方法；已改作 beego.Info() 调用；
2、把源代码里的 TODO 完成；
3、Export 的两个 URL 应该可以合并，用是否有 :id 来区分；这样 0 号文档输出整个 book 更顺；已完成；
4、统一代码风格，空格、命名之类的；每修改一个接触到的文件都会完成；
5、美化 PDF 的输出格式；
6、自动登录新开标签页而且并不能跳转至起始请求页的问题；已完成；
7、用户分组管理；
8、[自动]展示历史版本（已部分完成）；增强历史版本对比显示的能力；
9、[自动]展示作者信息（已完成）；也许可以和上一需求合并考虑；
10、查看了解评论功能；
11、Word 文件导出（可以考虑使用 pandoc，或者纯前端方案）；
12、PDF 附件预览（可以考虑使用 pdf.js）；
13、流程图支持（可以参考 https://github.com/benweet/stackedit）；

